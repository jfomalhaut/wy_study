1. 클론
=> 저장소에 등록된 프로젝트를 바로 다운로드할 수 있는 기능 (폴더까지 생성)
$ git clone [주소]

2. 풀 (다운로드)
=> 저장소에 등록된 자료를 업데이트 할 수 있는 기능
$ git pull

3. 푸쉬 (업로드)
=> 내 컴퓨터에 작업된 코드를 저장소에 등록하는 기능
$ git pull
$ git add .
$ git commit -m "message"
$ git push